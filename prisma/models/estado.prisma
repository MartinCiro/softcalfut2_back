model Estado {
  id          Int     @id @default(autoincrement())  
  nombre      String  @unique @map("nombre_estado")
  descripcion String?

  usuarios    Usuario[]
  productos   Productos[]

  @@map("estado") 
}
