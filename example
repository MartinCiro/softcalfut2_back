#DB env example
UserDB = user
PasswordDB = pass
ServerDB = host
Database = bd
PortDB = port  

# Config serve
env = type entor
PORT = port or default 4000

# Para el jwt
JWT_SECRETO = Your_Token_Secret

# Tiempo en el que expira el token si es 1h no es necesario (ver config)
JWT_TIEMPO_EXPIRA = 2h

# Parámetro de semilla del JWT
JWT_SALT = 8

